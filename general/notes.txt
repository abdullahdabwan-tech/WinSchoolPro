StageFeeItems


قتراحات مفيدة جدًا:
منع التكرار:
منع تكرار رسوم نفس المرحلة ونفس السنة ونوع الرسوم:

sql
نسخ الكود
CREATE UNIQUE INDEX IX_StageFeeItem_Unique
ON StageFeeItems(StageID, FeeTypeID, AcademicYearID);
حساب إجمالي الرسوم للمرحلة:

sql
نسخ الكود
CREATE PROCEDURE sp_StageFeeItem_GetTotalByStage
@StageID INT,
@AcademicYearID INT
AS
BEGIN
    SELECT SUM(Amount) AS TotalFees
    FROM StageFeeItems
    WHERE StageID = @StageID AND AcademicYearID = @AcademicYearID AND IsActive = 1;
END
إضافة واجهة بحث حسب المرحلة والسنة

إضافة عمود CreatedDate و CreatedBy لمتابعة من أضاف الرسوم ومتى.










✅ اقتراحات مهمة:
توحيد القيم الممكنة لـ Status عبر enum أو جدول Lookup (مثلاً: Pending, Paid, Overdue).

دالة تحديث الدفع الجزئي: مع حساب إن كان تم دفع كامل المبلغ أم لا وتغيير الحالة تلقائيًا.

إضافة إشعارات بالتقارير عن المستحقات المتأخرة.

دمج هذه الطبقة مع طبقات Student, StageFeeItem للربط السلس بين البيانات.



















rooms











💡 اقتراحات:
إضافة تحقق من تكرار اسم الغرفة قبل الإضافة أو التعديل.

إضافة إمكانية تحديث حالة الغرفة (متاحة/غير متاحة) تلقائيًا حسب الحجز.

إضافة خاصية تتبع السجلات (تاريخ الإنشاء، آخر تعديل، المستخدم المسؤول).

دعم خاصية البحث والتصفية حسب نوع الغرفة، الموقع، التوفر، وغيرها.

هل تريد إضافة أي تحسينات أو دوال خاصة؟
















results

ملاحظات واقتراحات:
التحقق من التكرار: يمكن إضافة دالة تتحقق إذا كان النتيجة موجودة لنفس الطالب وللنفس الامتحان لمنع التكرار.

إضافة دالة لتحديث غياب الطالب (IsAbsent) منفصلة.

تقييم النتيجة: يمكن إضافة خاصية محسوبة في Business Layer لحساب النسبة أو حالة النجاح/الرسوب.

إضافة دعم التصفية أو البحث في Data Access Layer حسب احتياجات النظام.

هل تريدني أضيف هذه الوظائف أو أي شيء إضافي؟








اطرح سؤالك على ChatGPT



ExamTypes

اقتراحات إضافية:
إضافة دالة تحقق إذا كان اسم نوع الامتحان موجود مسبقًا لتجنب التكرار.

إمكانية البحث (Search) في Data Access أو Business Layer حسب الاسم.

إضافة خاصية IsActive إذا تريد تفعيل/تعطيل أنواع الامتحانات.

هل تريد أضيف لك دوال تحقق أو بحث أو تفعيل/تعطيل؟







يوجد اعدات في agent لتحديث حالة الدفع للطالب كل اسبوع












تنبيهات الاشعرات مثل الطلاب المتأخرين في الرسوم مثل انتهاء حجز القاعات 







ايام الدوام تحتاج تعديل في trigger